{
	"name": "cra-new",
	"version": "0.1.0",
	"private": true,
  "homepage" : "http://cra-new.npmstack.com",
	"engines": {
		"node": ">=8"
	},
	"dependencies": {
		"http-server": "^0.10.0",
		"react": "^15.6.1",
		"react-dom": "^15.6.1"
	},
	"devDependencies": {
		"react-scripts": "1.0.7",
		"sw-precache": "^5.2.0"
	},
	"scripts": {
		"react-scripts": "react-scripts",
		"start": "react-scripts start",
		"build": "react-scripts build",
		"test": "react-scripts test --env=jsdom",
		"eject": "react-scripts eject",
		"deploy": "node http-server -a '0.0.0.0' -p '5000' /dist"
	}
}
